<% include ../../partials/layout.ejs %>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3 mx-auto">

    	<h1><i class="fas fa-sign-in-alt"></i>Nueva Materia Prima</h1>

    	<% if(messages.length > 0) { %>
    		<div class="alert alert-danger">
    			<% messages.forEach(function(message) { %>
    				<p><%= message %></p>
    			<% }); %>
    		</div>
    	<% } %>

    	<!-- MATERIEPRIME FORM -->
    	<form action="/app/inventario" method="post" enctype="multipart/form-data">
        <div class="form-group">
          <label>Producto</label>
          <input type="text" class="form-control" name="mp">
        </div>
        <div class="form-group">
          <label>Presentacion</label>
          <input type="text" class="form-control" name="presentacion">
        </div>
        <div class="form-group">
          <label>Rango</label>
          <input type="number" class="form-control" name="rango">
        </div>
    		<button type="submit" class="btn btn-success btn-lg">Guardar</button>
    	</form>

    	<hr>
    </div>
  </div>
  </div>
</div>
